import css from './Footer.module.css';
const Footer = () => {
  return (
    <footer className={css.footer}>
      <h1 className={css.title}>CHAINSTAFF</h1>
    </footer>
  );
};

export default Footer;
